{
  "name": "tailwinds2",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:css": "tailwindcss build src/style.css -o dist/styles.css",
    "watch:tailwind": "nodemon --watch src/style.css -x \"npm run build:css\"",
    "serve": "npm run watch:tailwind",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "nodemon": "^2.0.11",
    "tailwindcss": "^2.2.4"
  }
}
